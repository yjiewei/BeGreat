## Shell专项练习题目：选择判断

> 牛客网题目太少了。

```
1.以下不合法的shell头是(不合法指运行会报错)：
正确答案: C   你的答案: C (正确)
#!/bin/bash
#-/bin/bash 
!#/bin/bas

B选项其实算是注释，不能算严格意义的shell头
C就完全是错误的。
```

```
2.if [ $2 -a $2 = "test" ]中 -a是什么意思
正确答案: D   你的答案: D (正确)
大于
减
全部
并且
```

```
3.在 bash shell 环境下，当一命令正在执行时，按下 control-Z 会：
正确答案: C   你的答案: A (错误)
中止前台任务
给当前文件加上EOF
将前台任务转入后台
注销当前用户

ctrl+c 是终止当前任务
ctrl+z 表示前台任务转去后台，通常用来挂起一个进程
```

```
4.shell脚本中，那种方法可以判断文件test.sh是否为可执行文件（）
正确答案: B   你的答案: B (正确)
[-e test.txt]
[-x test.txt]
[-f test.txt]
[-d test.txt]

-x 可执行 execute
-r 可读   read
-w 写     write
-e 是否存在  exist 
-f 是否存在且为文件 file
-d 是否存在且为目录 dictionary
另外，用touch创建的是文件，用mkdir创建的是目录
```

```
5.shell中$0, $#, $*, $@, $?, $$的含义
$0    当前脚本的文件名
$n    传递给脚本或函数的参数。n 是一个数字，表示第几个参数。例如，第一个参数是$1，第二个参数是$2。
$#    传递给脚本或函数的参数个数。
$*    传递给脚本或函数的所有参数。
$@    传递给脚本或函数的所有参数。
$?    上个命令的退出状态，或函数的返回值。
$$    当前Shell进程ID。对于 Shell 脚本，就是这些脚本所在的进程ID。
$!    Shell最后运行的后台Process的PID
$-    显示Shell使用的当前选项，与set命令功能相同。

$* 和 $@ 的区别        $* 和 $@ 都表示传递给函数或脚本的所有参数，不被双引号(" “)包含时，都以”$1" “2&quot;…&quot;2"…"n” 的形式输出所有参数
当它们被双引号(" “)包含时，”$*" 会将所有的参数作为一个整体，以"$1 $2 … n&quot;的形式输出所有参数；&quot;n"的形式输出所有参数；"@" 会将各个参数分开，以"$1" “2&quot;…&quot;2"…"n” 的形式输出所有参数。
```

```
6.下面代码执行后输出为（ ）
#!/bin/bash
array[0]=H
array[1]=E
array[2]=E
array[3]=L
array[4]=O
echo "${#array[*]}"

正确答案: B   你的答案: C (错误)
4
5
H E E L O  // echo "${array[*]}"
0 1 2 3 4
```

